version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - apt-get update -y
      - apt-get install wget unzip -y
      # - export PACKER_RELEASE="1.6.6"
      # - wget https://releases.hashicorp.com/packer/1.6.6/packer_1.6.6_linux_amd64.zip
      # - unzip -f packer_1.6.6_linux_amd64.zip
      # - cp -r packer /usr/local/bin
      # - chown root:root /usr/local/bin/packer
      # - chown 777 /usr/local/bin/packer
      # - ./packer version
      # - /usr/local/bin/packer -version
      # - ./packer validate packer/iopp-base-ami.json     
      - echo "Installing Packer"
      - curl -o packer.zip https://releases.hashicorp.com/packer/1.6.6/packer_1.6.6_linux_amd64.zip
      - pwd
      - ls
      - unzip packer.zip
      - pwd
      - ls
      - chown root:root packer
      - chown 777 packer
      - ls -lash packer
      - echo "Validating Packer template"      
      - ./packer validate packer-files/iopp-base-ami.json     
      - pip install --upgrade pip
      - pip install --upgrade awscli
  build:
    commands:
      - echo "Hello Inspector"
      - packer build -var 'created-by=codepipeline' -var "branch_name=$GIT_BRANCH" packer-files/iopp-base-ami.json
